{
  "version": 2,
  "buildCommand": "cd dashboard && npm install && CI=false npm run build",
  "outputDirectory": "dashboard/build",
  "installCommand": "cd dashboard && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
